{
  "mcpServers": {
    "cowart_mcp": {
      "title": "Cowart Canvas Widget MCP",
      "description": "Core Cowart canvas MCP server. Use render_cowart_canvas_widget to open, reopen, or explicitly refresh the project canvas; use the state and insertion tools without rerendering when a Cowart widget is already open.",
      "command": "node",
      "args": [
        "./scripts/start-mcp.mjs"
      ],
      "cwd": "."
    }
  }
}
